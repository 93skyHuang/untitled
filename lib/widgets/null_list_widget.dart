import 'package:untitled/basic/include.dart';

class NullListWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Center(
      child: Text(
        '空空如也！！！',
        style: TextStyle(color: MyColor.mainColor, fontSize: 24),
      ),
    );
  }
}
